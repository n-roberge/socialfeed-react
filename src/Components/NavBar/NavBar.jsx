import './NavBar.css'

const NavBar = (props) => {
    return (  
        <nav className="navbar navbar-dark bg-dark">
            <span className="navbar-text">
                SocialFeed
            </span>
        </nav>
    );
}
 
export default NavBar;